## Binary Search Tree

Build a `TreeNode` and `BinarySearchTree` class that you can use to construct a BST.
Implement `insert` and `search` methods. Searching should return true if something exists, false otherwise.
Add methods that do Pre-Order, Post-Order,and In-order traversal and print results, verify that in order traverse produces a list of sorted elements.

## Solution

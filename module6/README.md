## Graph Traversal

Using an adjacency list  to represent an unweighted graph, implement `breadthFirstSearch` and `depthFirstSearch`.
Create a `findPath(start_node, end_node)` method that uses BFS to find and return the shortest path between two nodes in the graph.
If no path exists, it should return an indication of that.


## Solution
